# Packages mkenv installs into the orchestrator's own virtual environment.
# Keep in step with [project.dependencies] and the dev extra in pyproject.toml.
typer
pydantic
pyyaml
httpx
jinja2
rich
structlog
# development
pytest
pytest-asyncio
ruff
mypy
types-PyYAML
